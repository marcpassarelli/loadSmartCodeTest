import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  titleTextInput: { marginBottom: 5 },
  textInput: { borderWidth: 1, borderRadius: 5, height: 40, paddingLeft: 5 },
});

export default styles;
