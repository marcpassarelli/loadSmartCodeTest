import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  titleTextInput: { marginBottom: 5, fontWeight: 'bold', fontSize: 16 },
  textInput: {
    borderWidth: 1.5,
    borderRadius: 5,
    height: 40,
    paddingLeft: 5,
    backgroundColor: 'rgba(255,255,255,0.4)',
  },
});

export default styles;
